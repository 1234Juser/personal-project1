{{>layouts/header}}
<table class="table">
    <thead>
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>

    </tr>
    </thead>
    <tbody>
    {{#article}}
    <tr>
        <th>{{id}}</th>
        <td>{{title}}</td>
        <td>{{content}}</td>
    </tr>
    {{/article}}
    </tbody>
</table>
<a href="/first/{{article.id}}/edit" class="btn btn-primary">편집</a>
<a href="/first/{{article.id}}/delete" class="btn btn-primary">삭제</a>
<a href="/first">Go first</a>
{{>comments/_comments}}
{{>layouts/footer}}